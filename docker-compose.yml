services:
  app:
    build:
      context: .
      dockerfile: public/Dockerfile
    image: character-calculator:latest
    container_name: character-calc
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./database:/app/database